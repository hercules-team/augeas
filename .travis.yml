sudo: true
dist: trusty
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-6
      - libxml2-dev
      - libreadline-dev
      - valgrind
install:
  - sudo add-apt-repository ppa:jonathonf/binutils -y
  - sudo apt-get update
  - sudo apt-get install --only-upgrade binutils -y
  - sudo apt-get install libselinux1-dev
  - ./autogen.sh || printf "An error occured while running autogen. Exit status is $?." && cat config.log && exit 1
  - sed -i '41s/ENOENT/ENOENT || errno == EINVAL/' gnulib/tests/test-readlink.h
language: c
compiler: gcc-6
notifications:
  email: false
  irc:
    channels:
      - "irc.freenode.org#augeas"
script: ./configure --enable-deubg=yes && make && make check && ./src/try valgrind
