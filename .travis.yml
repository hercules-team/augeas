sudo: true
dist: trusty
install:
  - ./ci/install.sh
language: c
compiler: gcc-6
notifications:
  email: false
  irc:
    channels:
      - "irc.freenode.org#augeas"
script: ./configure --enable-deubg=yes && make && make check && ./src/try valgrind
