sudo: true
dist: trusty
cache:
  ccache: true
  directories:
    - $TRAVIS_BUILD_DIR/gnulib
install:
  - ./ci/install.sh
language: c
compiler:
  - gcc-6
notifications:
  email: false
  irc:
    channels:
      - "irc.freenode.org#augeas"
script: ./ci/test.sh
